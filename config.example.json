{
  "_comment": "Open Agents Bridge 配置文件示例",
  "_docs": "运行 'open-agents pair --name <device-name>' 添加设备",

  "_serverUrl_comment": "服务器地址，根据环境选择",
  "serverUrl": "wss://open-agents-api.binoctal.workers.dev",

  "_environment_comment": "可选: development, staging, production。不设置则自动检测",
  "environment": "",

  "_logLevel_comment": "可选: debug, info, warn, error。默认: info",
  "logLevel": "info",

  "cliEnabled": {
    "_comment": "启用的 CLI 工具",
    "claude": true,
    "cline": true,
    "codex": true,
    "gemini": true,
    "kiro": true
  },

  "permissions": {
    "_comment": "默认权限设置",
    "fs_read": true,
    "fs_write": true,
    "execute_bash": true,
    "network": false
  }
}
